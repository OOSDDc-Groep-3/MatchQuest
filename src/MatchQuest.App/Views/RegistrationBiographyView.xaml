<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:vm="clr-namespace:MatchQuest.App.ViewModels"
             xmlns:sys="clr-namespace:System;assembly=netstandard"
             x:Class="MatchQuest.App.Views.RegistrationBiographyView"
             x:DataType="vm:RegistrationBiographyViewModel"
             BackgroundImageSource="background.png"
             BackgroundColor="Transparent">

    <Grid RowDefinitions="*" ColumnDefinitions="*">
        <BoxView Margin="0"
                 Grid.Column="0"
                 Grid.ColumnSpan="2"
                 Color="#202022"
                 Opacity="0.7"
                 HorizontalOptions="Fill"
                 VerticalOptions="Fill"
                 InputTransparent="True" />

        <Border Margin="40"
                Grid.Column="0"
                Grid.ColumnSpan="2"
                Background="#202022"
                Opacity="0.8"
                Stroke="#c0ffee"
                StrokeShape="RoundRectangle 30"
                StrokeThickness="3"
                HorizontalOptions="Fill"
                VerticalOptions="Fill"
                InputTransparent="True" />

        <Image Margin="-1200,-700,-50,-50" Source="matchquest.png" VerticalOptions="Center" HorizontalOptions="Center" WidthRequest="250" HeightRequest="250"/>

        <ScrollView>
            <StackLayout Spacing="10" Padding="450,-100,450,450" VerticalOptions="Center">
                <Label Text="REGISTER" Margin="0,0,0,50" FontAttributes="Bold" TextColor="#c0ffee" FontSize="40" VerticalOptions="Center" HorizontalOptions="Center" />

                <!--
                <Label Text="First name" FontAttributes="Bold" TextColor="#FFFFFF" FontSize="20" HorizontalOptions="Start" />
                <Entry Placeholder="First name" FontAttributes="Bold" TextColor="#383535" BackgroundColor="#C6C2C2" Text="{Binding FirstName}" />

                <Label Text="Last name" FontAttributes="Bold" TextColor="#FFFFFF" FontSize="20" HorizontalOptions="Start" />
                <Entry Placeholder="Last name" FontAttributes="Bold" TextColor="#383535" BackgroundColor="#C6C2C2" Text="{Binding LastName}" />

                <Label Text="Region" FontAttributes="Bold" TextColor="#FFFFFF" FontSize="20" HorizontalOptions="Start" />
                <Entry Placeholder="Region" FontAttributes="Bold" TextColor="#383535" BackgroundColor="#C6C2C2" Text="{Binding Region}" />

                <Label Text="Date of birth" FontAttributes="Bold" TextColor="#FFFFFF" FontSize="20" HorizontalOptions="Start" />
                <DatePicker Date="{Binding BirthDate}" FontAttributes="Bold" TextColor="#383535" BackgroundColor="#C6C2C2" MaximumDate="{x:Static sys:DateTime.Now}" />

                <Label Text="{Binding LoginMessage}" TextColor="Red" IsVisible="{Binding LoginMessage, Converter={StaticResource StringNotNullOrEmptyToBool}}" />
                -->
                <Button BackgroundColor="#c0ffee" FontSize="20" FontAttributes="Bold" TextColor="#383535" Text="Confirm" Command="{Binding NextCommand}" Margin="0,20,0,0"  />
            </StackLayout>
        </ScrollView>
    </Grid>
</ContentPage>