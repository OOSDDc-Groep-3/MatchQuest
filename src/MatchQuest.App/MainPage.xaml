<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             x:Class="MatchQuest.App.MainPage">

    <!--
      Pseudocode / Plan (detailed):
      - Wrap the existing right-column content (the `VerticalStackLayout`) inside a `Border` control.
      - Move the existing padding from `VerticalStackLayout` to the `Border.Padding` so spacing is applied inside the border.
      - Configure the `Border` with:
        - `Stroke` color for the border line (e.g., #CCCCCC to match list item borders).
        - `StrokeThickness` (e.g., 1) to make the border visible.
        - `CornerRadius` (e.g., 8) for a rounded border look.
        - `BackgroundColor` set to Transparent to avoid changing existing visuals.
      - Keep the inner `VerticalStackLayout` content unchanged except removing its `Padding` to avoid double-padding.
      - Ensure `Grid.Column="1"` is applied to the `Border` so layout remains the same.
      - Preserve all existing handlers, names, semantic properties, and styles.
    -->

    <ScrollView>
        <Grid HorizontalOptions="Fill" VerticalOptions="Fill" Padding="50">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="1.5*" />
                <ColumnDefinition Width="2.5*" />
            </Grid.ColumnDefinitions>

            <!-- Left column ~1/4: Chatlist -->
            <CollectionView
                Grid.Column="0"
                SelectionMode="Single"
                HorizontalOptions="Fill"
                VerticalOptions="Start">
                <CollectionView.ItemsLayout>
                    <LinearItemsLayout Orientation="Vertical" ItemSpacing="8" />
                </CollectionView.ItemsLayout>

                <CollectionView.ItemTemplate>
                    <DataTemplate>
                        <Border
                            Padding="1"
                            Stroke="#C0ffee"
                            StrokeThickness="4"
                            BackgroundColor="Transparent">
                            <Label Text="{Binding .}"
                                   FontSize="35"
                                   VerticalOptions="Center" />
                        </Border>
                    </DataTemplate>
                </CollectionView.ItemTemplate>

                <CollectionView.ItemsSource>
                    <x:Array Type="{x:Type x:String}">
                        <x:String>Match 1</x:String>
                        <x:String>Match 2</x:String>
                        <x:String>Match 3</x:String>
                        <x:String>Match 4</x:String>
                        <x:String>Match 5</x:String>
                        <x:String>Match 6</x:String>
                        <x:String>Match 7</x:String>
                        <x:String>Match 8</x:String>
                        <x:String>Match 9</x:String>
                        <x:String>Match 11</x:String>
                        <x:String>Match 11</x:String>
                        <x:String>Match 11</x:String>
                        <x:String>Match 11</x:String>
                    </x:Array>
                </CollectionView.ItemsSource>
            </CollectionView>

            <!-- Right column ~3/4: bordered container wrapping existing content -->
            <Border
                Grid.Column="1"
                Stroke="#C0ffee"
                StrokeThickness="3"
                BackgroundColor="Transparent"
                Padding="0">
                <Border.StrokeShape>
                    <RoundRectangle CornerRadius="175"/>
                </Border.StrokeShape>
                <VerticalStackLayout
                    
                    BackgroundColor="Transparent"
                    Spacing="70">
                    <Image
                        Source="https://icon-library.com/images/blank-person-icon/blank-person-icon-14.jpg"
                        HeightRequest="300"
                        LengthRe
                        Aspect="AspectFit"
                        HorizontalOptions="Fill"
                        SemanticProperties.Description="dot net bot in a submarine number ten" />

                    <Label
                        Text="Hello, World!"
                        Style="{StaticResource Headline}"
                        SemanticProperties.HeadingLevel="Level1" />

                    <Label
                        Text="Welcome to &#10;.NET Multi-platform App UI"
                        Style="{StaticResource SubHeadline}"
                        SemanticProperties.HeadingLevel="Level2"
                        SemanticProperties.Description="Welcome to dot net Multi platform App U I" />

                    <!-- Download images.png voor like en deslike buttons -->
                    <HorizontalStackLayout
                        Spacing="130"
                        HorizontalOptions="Center"
                        VerticalOptions="Center">

                        <ImageButton
                            x:Name="DeslikeBtn"
                            Source="https://purepng.com/public/uploads/large/red-dislike-symbol-emoji-pc5.png"
                            SemanticProperties.Hint="Counts the number of times you click"
                            SemanticProperties.Description="Dislike"
                            Clicked="OnCounterClicked"
                            HeightRequest="60"
                            WidthRequest="60"
                            CornerRadius="30"
                            Padding="0"
                            BackgroundColor="Transparent" />

                        <ImageButton
                            x:Name="LikeBtn"
                            Source="https://cdn.creazilla.com/icons/3268993/round-green-thumbs-up-like-button-icon-lg.png"
                            SemanticProperties.Hint="Counts the number of times you click"
                            SemanticProperties.Description="Like"
                            Clicked="OnCounterClicked"
                            HeightRequest="60"
                            WidthRequest="60"
                            CornerRadius="30"
                            Padding="0"
                            BackgroundColor="Transparent" />
                    </HorizontalStackLayout>
                </VerticalStackLayout>
            </Border>
        </Grid>
    </ScrollView>

</ContentPage>
